apiVersion: v1
kind: Service
metadata:
  name: {{ include "{{ProjectNameKebab}}.fullname" . }}
  labels:
    {{- include "{{ProjectNameKebab}}.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "{{ProjectNameKebab}}.selectorLabels" . | nindent 4 }}
