// Compile services have been consolidated into StackAlchemist.Engine.Services for Phase 4.
// The Engine now hosts the CompileWorkerService as an in-process BackgroundService.
// This file is intentionally empty; it is preserved so the Worker project continues
// to build as a standalone host for future separate-process deployments.
