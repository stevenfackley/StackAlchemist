import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Home",
  description:
    "StackAlchemist turns natural language into fully compiled, downloadable code repositories.",
};

export { default } from "./HomePageClient";
